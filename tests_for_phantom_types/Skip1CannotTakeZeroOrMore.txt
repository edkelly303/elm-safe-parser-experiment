-- TYPE MISMATCH --------------------------------- Skip1CannotTakeZeroOrMore.elm

The 1st argument to `skip1` is not what I expect:

7|     SP.skip1 (SP.succeed ())
                 ^^^^^^^^^^^^^
This `succeed` call produces:

    SP.Parser SP.ZeroOrMore ()

But `skip1` needs the 1st argument to be:

    SP.Parser SP.OneOrMore ()

