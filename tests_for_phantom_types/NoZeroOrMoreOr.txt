-- TYPE MISMATCH -------------------------------------------- NoZeroOrMoreOr.elm

This function cannot handle the argument sent through the (|>) pipe:

7|     SP.succeed ()
8|         |> SP.or (SP.succeed ())
              ^^^^^^^^^^^^^^^^^^^^
The argument is:

    SP.Parser SP.ZeroOrMore ()

But (|>) is piping it to a function that expects:

    SP.Parser SP.OneOrMore ()

